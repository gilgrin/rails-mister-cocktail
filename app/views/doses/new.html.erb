<h1>Add a new dose</h1>

<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient %>
  <%= f.submit %>
<% end %>

<%= link_to "Back to cocktail", cocktail_path(@cocktail), class: 'btn btn-primary mt-2' %>
